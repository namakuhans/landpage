--- src/components/CurvedMarquee.jsx
+++ src/components/CurvedMarquee.jsx
@@ -45,15 +45,15 @@
   });

   if (!width) {
-    return <div ref={containerRef} className="w-full h-[300px] bg-[#0f1115]" />;
+    return <div ref={containerRef} className="w-full h-[250px] bg-[#0f1115]" />;
   }

-  const SVG_HEIGHT = 400;
+  const SVG_HEIGHT = 350;

   return (
     <div
       ref={containerRef}
-      className="w-full relative overflow-hidden h-[350px] bg-[#0f1115] flex items-start justify-center"
+      className="w-full relative overflow-hidden h-[250px] bg-[#0f1115] flex items-start justify-center"
     >
       <svg
         width={width}
@@ -63,12 +63,12 @@
         style={{ overflow: 'visible' }}
       >
         {/*
-          A gentler 200px drop across the visible screen (x=0 to x=width) based on user feedback.
-          p1y - p0y = 10000 ensures exactly a 200px drop for our t-values.
+          A gentler 150px drop across the visible screen (x=0 to x=width).
+          p1y - p0y = 7500 ensures exactly a 150px drop for our t-values.
         */}
         <path
           id="curve-path"
-          d={`M ${-width * 2} -4500 Q ${width / 2} 5500 ${width * 3} -4500`}
+          d={`M ${-width * 2} -3500 Q ${width / 2} 4000 ${width * 3} -3500`}
           fill="transparent"
         />
