--- src/components/CurvedMarquee.jsx
+++ src/components/CurvedMarquee.jsx
@@ -37,14 +37,14 @@
       let newOffset = prev - move;
       if (newOffset <= -textWidth) {
-        newOffset += textWidth;
+        newOffset = newOffset % textWidth;
       }
       return newOffset;
     });
   });

   if (!width) {
-    return <div ref={containerRef} className="w-full h-[400px] bg-[#0f1115]" />;
+    return <div ref={containerRef} className="w-full h-[250px] bg-[#0f1115]" />;
   }

-  const SVG_HEIGHT = 600;
+  const SVG_HEIGHT = 350;

   return (
     <div
       ref={containerRef}
-      className="w-full relative overflow-hidden h-[600px] bg-[#0f1115] flex items-start justify-center"
+      className="w-full relative overflow-hidden h-[300px] bg-[#0f1115] flex items-start justify-center"
     >
       <svg
         width={width}
         height={SVG_HEIGHT}
         viewBox={`0 0 ${width} ${SVG_HEIGHT}`}
         className="absolute top-0 left-0 pointer-events-none"
         style={{ overflow: 'visible' }}
       >
-        {/*
-          A mathematically perfect 400px drop across the visible screen (x=0 to x=width).
-          By placing the ends at -2*width and 3*width, we maintain the parabola's shape.
-          The Y coordinates are constant because the vertical drop is independent of width.
-        */}
         <path
           id="curve-path"
-          d={`M ${-width * 2} -9500 Q ${width / 2} 10500 ${width * 3} -9500`}
+          d={`M ${-width * 2} -4500 Q ${width / 2} 5500 ${width * 3} -4500`}
           fill="transparent"
         />

-        <text className="text-4xl md:text-5xl font-bold fill-white/80" style={{ letterSpacing: '0.05em' }}>
-          <textPath href="#curve-path" startOffset={offset}>
+        <text className="text-3xl md:text-4xl font-bold fill-white/80" style={{ letterSpacing: '0.05em' }}>
+          <textPath href="#curve-path" startOffset={offset - textWidth}>
+            {repeatedText}
+          </textPath>
+          <textPath href="#curve-path" startOffset={offset}>
+            {repeatedText}
+          </textPath>
+          <textPath href="#curve-path" startOffset={offset + textWidth}>
             {repeatedText}
           </textPath>
         </text>

         <text
           ref={measureRef}
-          className="text-4xl md:text-5xl font-bold opacity-0"
+          className="text-3xl md:text-4xl font-bold opacity-0"
         >
           {baseText}
         </text>
